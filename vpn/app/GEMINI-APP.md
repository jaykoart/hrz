# Horizon VPN Desktop Application 개발 가이드

## 📌 Overview
Horizon VPN 데스크톱 애플리케이션은 사용자가 VPN 노드로 참여하고, VPN 서비스를 이용할 수 있는 크로스 플랫폼 클라이언트입니다.

---

## 🎯 핵심 기능

### 사용자 모드 (Consumer Mode)
> VPN을 사용하여 익명성/지역 우회
- 글로벌 노드 목록 조회 및 선택
- 원클릭 VPN 연결
- 연결 상태 및 속도 모니터링
- 설정 (Wi-Fi 전용, 자동 연결 등)

### 노드 모드 (Node Provider Mode)
> 유휴 대역폭 공유로 네트워크 기여
- 노드 등록 및 활성화
- 대역폭 공유 설정 (제한, Wi-Fi 전용)
- 기여 통계 대시보드
- 자원 사용량 모니터링

---

## 🛠️ 기술 스택

| 분류 | 기술 | 이유 |
|------|------|------|
| **프레임워크** | Tauri 2.0 | 경량 (Electron 대비 ~10MB), Rust 기반 보안 |
| **프론트엔드** | React + TypeScript | 기존 웹 프론트엔드와 코드 공유 가능 |
| **스타일링** | CSS Modules | 컴포넌트 단위 스타일 관리 |
| **상태 관리** | Zustand | 경량, 간단한 API |
| **VPN 코어** | WireGuard-Go | 크로스 플랫폼, 고성능 |
| **시스템 트레이** | Tauri native | 네이티브 통합 |

---

## 🖥️ 지원 플랫폼

| 플랫폼 | 우선순위 | 상태 |
|--------|---------|------|
| **Windows** | P0 (최우선) | [ ] 미착수 |
| **macOS** | P0 (최우선) | [ ] 미착수 |
| **Linux** | P1 | [ ] 미착수 |

---

## ✅ 개발 체크리스트

### Phase 1: 프로젝트 초기 설정
> Tauri + React 환경 구축
- [ ] Tauri 2.0 프로젝트 생성
- [ ] React + TypeScript 설정
- [ ] 기본 윈도우 구성 (크기, 타이틀바, 아이콘)
- [ ] 시스템 트레이 아이콘 설정
- [ ] 자동 업데이트 설정 (Tauri Updater)

### Phase 2: 인증 및 사용자 관리
> Google OAuth 연동
- [ ] 딥링크 기반 OAuth 플로우
- [ ] 로컬 토큰 저장 (Keychain/Credential Manager)
- [ ] 자동 로그인 (저장된 토큰 사용)
- [ ] 로그아웃 기능
- [ ] 사용자 프로필 표시

### Phase 3: VPN 연결 기능 (Consumer Mode)
> WireGuard 기반 VPN 연결
- [ ] 노드 목록 조회 UI
  - [ ] 국가별 필터링
  - [ ] 지연시간(Ping) 표시
  - [ ] 추천 노드 표시
- [ ] VPN 연결/해제 기능
  - [ ] WireGuard 터널 생성
  - [ ] 시스템 라우팅 테이블 수정
  - [ ] DNS 설정 변경
- [ ] 연결 상태 표시
  - [ ] 연결 시간
  - [ ] 현재 IP 주소
  - [ ] 업/다운로드 속도

### Phase 4: 노드 제공 기능 (Provider Mode)
> P2P 네트워크 기여
- [ ] 노드 등록 API 연동
- [ ] 백그라운드 서비스 실행
- [ ] 대역폭 공유 설정
  - [ ] Wi-Fi 전용 모드
  - [ ] 최대 대역폭 제한
  - [ ] 시간대별 설정
- [ ] 기여 통계 대시보드
  - [ ] 총 공유량
  - [ ] 현재 연결 수

### Phase 5: 시스템 통합
> OS 레벨 통합
- [ ] 시스템 시작 시 자동 실행
- [ ] 시스템 트레이 기능
  - [ ] 연결/해제 빠른 액션
  - [ ] 상태 표시 아이콘
- [ ] 알림 시스템
  - [ ] 연결/해제 알림
  - [ ] 업데이트 알림
- [ ] 관리자 권한 요청 (VPN 설정용)

### Phase 6: UI/UX 개선
> 프리미엄 디자인 적용
- [ ] 다크/라이트 테마
- [ ] GPU 가속 지도 (노드 위치 표시)
- [ ] 애니메이션 전환 효과
- [ ] 접근성 (키보드 네비게이션)
- [ ] 다국어 지원 (i18n)

### Phase 7: 테스트 및 배포
> QA 및 배포 파이프라인
- [ ] 유닛 테스트 작성
- [ ] E2E 테스트 (WebDriver)
- [ ] 코드 서명 설정
  - [ ] Windows: Authenticode
  - [ ] macOS: Apple Developer ID
- [ ] 자동 업데이트 서버 구성
- [ ] GitHub Releases 배포

---

## 📂 디렉토리 구조 (예정)
```
vpn/app/
├── GEMINI-APP.md           # 본 문서
├── src-tauri/              # Tauri (Rust) 백엔드
│   ├── Cargo.toml
│   ├── src/
│   │   ├── main.rs         # 엔트리포인트
│   │   ├── commands/       # Tauri 커맨드
│   │   │   ├── auth.rs     # OAuth 처리
│   │   │   ├── vpn.rs      # VPN 연결 관리
│   │   │   └── node.rs     # 노드 등록/상태
│   │   └── wireguard/      # WireGuard 통합
│   ├── icons/              # 앱 아이콘
│   └── tauri.conf.json     # Tauri 설정
├── src/                    # React 프론트엔드
│   ├── App.tsx
│   ├── components/
│   │   ├── TitleBar/       # 커스텀 타이틀바
│   │   ├── Sidebar/        # 네비게이션
│   │   ├── NodeList/       # 노드 목록
│   │   ├── ConnectionStatus/
│   │   └── Settings/
│   ├── pages/
│   │   ├── Connect.tsx     # VPN 연결 화면
│   │   ├── Nodes.tsx       # 노드 제공 화면
│   │   ├── Stats.tsx       # 통계 대시보드
│   │   └── Settings.tsx
│   ├── stores/             # Zustand 상태
│   ├── hooks/              # 커스텀 훅
│   ├── services/           # API 통신
│   └── styles/             # CSS Modules
├── package.json
└── tsconfig.json
```

---

## 🔗 백엔드 API 연동

### 필요 API 목록
| API | 메서드 | 용도 |
|-----|--------|------|
| `/api/auth/google` | POST | Google OAuth 로그인 |
| `/api/user/profile` | GET | 사용자 정보 조회 |
| `/api/nodes` | GET | 노드 목록 조회 |
| `/api/nodes/register` | POST | 노드 등록 |
| `/api/nodes/heartbeat` | POST | 노드 상태 갱신 |
| `/api/vpn/keys` | POST | WireGuard 키 교환 |
| `/api/vpn/peer-config` | GET | 피어 설정 조회 |
| `/api/consent` | GET/PUT | 동의 상태 관리 |
| `/api/settings` | GET/PUT | 사용자 설정 |

---

## 🎨 UI 와이어프레임

### 메인 화면 (연결 전)
```
┌───────────────────────────────────────┐
│ ─ □ ✕   Horizon VPN                  │
├───────────────────────────────────────┤
│  ┌─────┐                              │
│  │ 🌐  │  연결 안됨                    │
│  └─────┘                              │
│                                       │
│  ┌─────────────────────────────────┐  │
│  │    [   연결하기   ]              │  │
│  └─────────────────────────────────┘  │
│                                       │
│  🇰🇷 한국 - 서울     ▼               │
│                                       │
├───────────────────────────────────────┤
│  🔗 연결  │  📊 통계  │  ⚙️ 설정      │
└───────────────────────────────────────┘
```

### 메인 화면 (연결 후)
```
┌───────────────────────────────────────┐
│ ─ □ ✕   Horizon VPN                  │
├───────────────────────────────────────┤
│  ┌─────┐                              │
│  │ 🔒  │  연결됨 • 00:12:34           │
│  └─────┘                              │
│                                        │
│  🇯🇵 일본 - 도쿄                      │
│  IP: 103.xxx.xxx.xxx                  │
│                                       │
│  ↓ 12.5 MB/s   ↑ 3.2 MB/s            │
│                                       │
│  ┌─────────────────────────────────┐  │
│  │    [   연결 해제   ]             │  │
│  └─────────────────────────────────┘  │
├───────────────────────────────────────┤
│  🔗 연결  │  📊 통계  │  ⚙️ 설정      │
└───────────────────────────────────────┘
```

---

## 🚀 개발 시작 명령어

### 1. Tauri 프로젝트 생성
```bash
cd /Users/wonjunjang/hrz/vpn/app
npm create tauri-app@latest . -- --template react-ts
```

### 2. 개발 서버 실행
```bash
npm run tauri dev
```

### 3. 프로덕션 빌드
```bash
npm run tauri build
```

---

## 📝 참고 문서
- [Tauri 2.0 공식 문서](https://v2.tauri.app/)
- [WireGuard-Go](https://github.com/WireGuard/wireguard-go)
- [Zustand 상태관리](https://zustand-demo.pmnd.rs/)
- `GEMINI-BACKEND.md`: 백엔드 API 명세

---

## 🔮 향후 확장 계획

### 모바일 앱 (Phase 8+)
- iOS: Swift + WireGuard NetworkExtension
- Android: Kotlin + WireGuard VPN Service

### 브라우저 확장 (Phase 9+)
- Chrome/Firefox Extension
- 프록시 전용 모드 (VPN 불필요)
